// Q1: Create two integer variables length and breadth and assign values then check if they are square values or rectangle values. ie: if both values are equal then it's square otherwise rectangle?

void main() {
  int length = 10;
  int breadth = 10;

// Check for square or rectangle
  if (length == breadth) {
    print("It's Square");
  } else {
    print("It's Rectangle");
  }
}
